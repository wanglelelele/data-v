{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "dev:parallel": "lerna run --parallel dev",
    "dev:editor": "lerna run dev --scope=editor",
    "test": "erna run test",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^5.3.0",
    "nx": "14.2.2",
    "unplugin-element-plus": "^0.4.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wanglelelele/data-v.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
