{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "dev:parallel": "npx lerna run --parallel dev",
    "dev:editor": "npx lerna run dev --scope=editor",
    "test": "npx lerna run test",
    "bootstrap": "npx lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^5.3.0",
    "nx": "14.2.2",
    "unplugin-element-plus": "^0.4.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wanglelelele/data-v.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
